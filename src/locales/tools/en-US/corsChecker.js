export default {
  title: 'CORS Checker',
  description: 'Check whether a target API allows cross-origin access from the browser perspective, with configuration advice',
  urlLabel: 'Target URL',
  urlPlaceholder: 'Enter the API to check, e.g. api.example.com/data',
  methodLabel: 'Request method',
  checkBtn: 'Check',
  checking: 'Checking, waiting up to 10 seconds…',
  customHeaderLabel: 'Send a custom header (triggers preflight)',
  customHeaderHint: 'When enabled, a non-simple header X-Test: 1 is attached to verify the server preflight (OPTIONS) configuration',
  errInvalidUrl: 'Please enter a valid HTTP or HTTPS URL',
  errCheckFailed: 'Something went wrong during the check, please try again later',
  resultTitle: 'Check result',
  resAllowed: 'CORS allowed: the response can be read cross-origin',
  resAllowedDesc: 'The browser successfully read the response, which means the server returned CORS headers allowing the current origin',
  resBlocked: 'CORS blocked: server reachable but the browser refuses to expose the response',
  resBlockedDesc: 'The server did respond (no-cors probe succeeded), but the response lacks CORS headers allowing the current origin, so the browser hides it from page scripts',
  resTimeout: 'Response timeout: the server did not finish within 10 seconds',
  resTimeoutDesc: 'The probe shows the server is reachable, but the regular cross-origin request did not finish within 10 seconds. The server may be slow or a middlebox is stalling the request',
  resUnreachable: 'Network unreachable: the request never reached the target server',
  resUnreachableDesc: 'Both the regular request and the no-cors probe failed. The domain may not resolve, the service may be down, the network may be restricted, or an HTTP target from an HTTPS page was blocked',
  corsHeadersTitle: 'Key CORS response headers',
  notReadable: 'Not readable by the browser',
  exposeNote: 'Note: the browser only exposes a few safe response headers (such as Cache-Control and Content-Type) by default. CORS headers like Access-Control-Allow-Origin become readable by scripts only when the server also sends Access-Control-Expose-Headers. "Not readable" does not mean the server omitted the header, only that this environment cannot verify it',
  readableHeadersTitle: 'Readable response headers',
  headerNameCol: 'Header',
  headerValueCol: 'Value',
  suggestionsTitle: 'Configuration advice',
  sugSameOrigin: 'The target shares the same origin as this page, so the same-origin policy does not apply and this result does not reflect a real cross-origin scenario',
  sugWildcardCredentials: 'Access-Control-Allow-Origin is the wildcard *. To send cookies or other credentials, it must be a concrete origin plus Access-Control-Allow-Credentials: true',
  sugAcoOk: 'The server allows this origin: ',
  sugCustomHeaderPassed: 'The request with custom header X-Test succeeded, so the preflight already allows that header',
  sugAddAco: 'Add Access-Control-Allow-Origin to the server response with the allowed origin, e.g. the current page origin: ',
  sugPreflight: 'Non-simple requests send an OPTIONS preflight first; the server must answer it with 2xx and the matching Access-Control-Allow-* headers',
  sugAllowMethods: 'The current method is not a simple method, Access-Control-Allow-Methods must include: ',
  sugPostJson: 'If the POST body sends JSON with Content-Type: application/json, a preflight is triggered and Content-Type must be listed in Access-Control-Allow-Headers',
  sugAllowHeaders: 'Access-Control-Allow-Headers must include the custom header: ',
  sugCheckDomain: 'First verify the domain spelling, port and protocol, and that the service is running',
  sugMixedContent: 'Requests to HTTP targets from an HTTPS page are blocked by the mixed-content policy; try an HTTPS target',
  sugTimeout: 'Consider reducing server response time or checking reverse proxy and firewall timeout settings, then retry',
  sugCredentials: 'To send cookies: set credentials to include on the frontend, the server must return Access-Control-Allow-Credentials: true, and Allow-Origin cannot be the wildcard',
  sugServerSide: 'This tool reflects the browser perspective only. Also verify the real response headers server-side with curl or similar, sending an Origin header',
  detailsToggle: 'Request / response details',
  detailFinalUrl: 'Final URL',
  detailStatus: 'Status code',
  statusHidden: 'Not readable (the browser hides the status code when blocked)',
  detailElapsed: 'Elapsed',
  detailProbe: 'no-cors probe',
  probeOk: 'Succeeded: the target server is reachable',
  probeFailed: 'Failed: the target is unreachable or the domain is invalid',
  probeSkipped: 'Skipped (the main request already succeeded)',
  detailError: 'Main request error',
  detailMethod: 'Request config',
  browserLimitNote: 'Browser limitation: a page can only read a response when the server replies with Access-Control-Allow-Origin allowing the current origin. This tool therefore reports CORS availability from the browser perspective — "blocked" may mean a missing header, an origin mismatch or a failed preflight. It cannot replace server-side configuration checks.',
  privacyNote: 'Privacy note: when you run a check, the target URL is sent as a real HTTP request to the target server through your network, with headers such as the current page Origin. Results are only shown on this page and are never stored or uploaded by this site',
}
