export default {
  title: 'SQL Dialect Converter',
  description: 'Convert SQL syntax between MySQL, PostgreSQL and SQLite with a rule engine that logs every hit',
  sourceDialect: 'Source dialect',
  targetDialect: 'Target dialect',
  mysql: 'MySQL',
  postgresql: 'PostgreSQL',
  sqlite: 'SQLite',
  sameDialectHint: 'Source and target dialects are the same. No conversion needed',
  inputLabel: 'SQL input',
  inputPlaceholder: 'Paste SQL statements, multiple statements supported…',
  outputLabel: 'Converted SQL',
  logTitle: 'Conversion log',
  colRule: 'Rule',
  colHits: 'Hits',
  colSnippet: 'Matched snippet',
  logEmpty: 'No conversion rules were hit',
  statsLabel: 'Total hits',
  ruleLimitComma: 'LIMIT offset syntax (comma to OFFSET)',
  ruleLimitOffset: 'LIMIT OFFSET syntax (to comma)',
  ruleAutoIncrement: 'Auto-increment primary key column',
  ruleSerial: 'SERIAL auto-increment type',
  ruleNowToCurrent: 'NOW() to CURRENT_TIMESTAMP',
  ruleCurrentToNow: 'CURRENT_TIMESTAMP to NOW()',
  ruleBacktickToQuote: 'Backtick identifiers to double quotes',
  ruleQuoteToBacktick: 'Double-quoted identifiers to backticks',
  ruleIfnullToCoalesce: 'IFNULL to COALESCE',
  ruleCoalesceToIfnull: 'COALESCE to IFNULL',
  ruleConcatToPipe: 'CONCAT to double-pipe concatenation',
  rulePipeToConcat: 'Double-pipe concatenation to CONCAT',
  ruleTypeMap: 'Data type mapping',
  ruleUnsigned: 'Remove UNSIGNED keyword',
  ruleEnum: 'ENUM type to TEXT',
  ruleTableOptions: 'Remove table options (ENGINE, CHARSET, etc.)',
  noteEscape: 'Escape hint: MySQL supports backslash escapes; PostgreSQL and SQLite need doubled single quotes',
  noteConcat: 'CONCAT with more than two arguments was not converted. Rewrite manually',
  notePgOnly: 'PostgreSQL-specific syntax found (type casts etc.). Review manually',
  escapeHint: 'Conversions are regex based without a full parser. Review the output before use',
  faq1q: 'Can the converted SQL run in production as is?',
  faq1a: 'Not recommended. Conversions are regex based without a full syntax parser, so complex statements may be missed. Check every hit in the conversion log and validate in a test environment first.',
  faq2q: 'What if the log shows no rules were hit?',
  faq2a: 'It means no convertible patterns were found, or the target dialect already accepts the syntax. PostgreSQL-specific syntax and CONCAT with more than two arguments are not rewritten automatically and need manual edits.',
}
